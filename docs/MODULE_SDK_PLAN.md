# Plugin SDK documentation moved

This former core development plan is retired. Canonical SDK, schema, testkit,
CLI, Devkit and Plugin-development documentation belongs under `devkit/` in the
separate `nad-plugins` repository.

Until the public snapshot exists, use the private Gitea `nad-plugins` working
repository. This file remains only as a compatibility target for older links;
it is not an active plan and does not override [`STATUS.md`](STATUS.md),
[`INITIAL_PUBLIC_RELEASE.md`](INITIAL_PUBLIC_RELEASE.md) or
[`ROADMAP.md`](ROADMAP.md).
